!!!
%html
  %head
    %meta
    %title
      Follow My Travel
    %meta{content: "initial-scale=1.0, width=device-width", name: "viewport"}
    = stylesheet_link_tag "application-error", :media => "all"
    = javascript_include_tag "application-error"
    = csrf_meta_tags
%body

  = yield

= render :partial => 'shared/ga' if Rails.env.production?
